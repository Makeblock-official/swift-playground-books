/*:
 This time, you will write your own function, called "swing()".
 
 Follow the same format as the function "jiggle()",
 think how should the robot swing, and write them with
 `moveForward()`, `moveBack()`, `moveLeft()`, `moveRight()`.
 
 Try them by yourself!
 
*/
//#-hidden-code

execiseCode = {


//#-code-completion(everything, hide)
//#-code-completion(keyword, show, func)
//#-code-completion(identifier, show, beepDo(), beepMi(), beepSol(), moveForward(), moveBack(), moveLeft(), moveRight(), lightLeft(color:), lightRight(color:), lightBoth(color:))
    //#-end-hidden-code
    
    func jiggle() {
        moveForward()
        moveBack()
        //#-editable-code You can add more actions
        //#-end-editable-code
    }
    
    //#-editable-code Write you code. Start with func
    //#-end-editable-code
//#-hidden-code
    
}

runWithCommands()

//#-end-hidden-code
