/*:
 You can move the mBot around with these commands: 
 * `moveForward()`: the mBot will move forward;
 * `moveBack()`: the mBot will move backward;
 * `moveLeft()`: the mBot will turn left;
 * `moveRight()`: the mBot will turn right.
 
 Try them by yourself!
 
*/
//#-hidden-code

execiseCode = {


//#-code-completion(everything, hide)
//#-code-completion(identifier, show, moveForward(), moveBack(), moveLeft(), moveRight())
    //#-end-hidden-code
//#-editable-code Tap to write your code
//#-end-editable-code
//#-hidden-code
    
}

runWithCommands()

//#-end-hidden-code
