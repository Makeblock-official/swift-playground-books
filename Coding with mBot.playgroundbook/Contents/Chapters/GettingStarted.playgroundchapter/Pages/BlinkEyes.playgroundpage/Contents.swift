/*:
 Two lights (LEDs) sits on top of the mBot
 You can use these commands to set their colors:
 * `lightLeft(color)` change the color of the mBot's left light;
 * `lightRight(color)` change the color of the mBot's right light;
 * `lightBoth(color)` change the color of both lights on the mBot.
 
 Try them by yourself!
 
*/
//#-hidden-code



execiseCode = {

//#-code-completion(everything, hide)
//#-code-completion(literal, show, color, array)
//#-code-completion(identifier, show, lightLeft(color:), lightRight(color:), lightBoth(color:))
    //#-end-hidden-code
//#-editable-code Tap to write your code
//#-end-editable-code
//#-hidden-code
    
}

runWithCommands()

//#-end-hidden-code
